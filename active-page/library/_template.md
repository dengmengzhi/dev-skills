# 新配方模板

复制下面整段到对应分类文件末尾，替换尖括号内容；再在 `index.md` 加一行。

````markdown
### <前缀-序号>  <中文名称>（<英文短名>）

- 区块：<导航 / 首屏 / 列表 / 卡片 / CTA / 表单 / 弹层 / 页脚 / 通用，可多选，用 / 分隔>
- 风格：<奢华 / 活泼 / 企业 / 内容 / 通用，可多选>
- 触发：<入场 / 滚动进入视口 / 滚动驱动 / hover / 点击反馈 / 数据变化 / 路由切换 / 状态过渡>
- 端：<PC / H5 / 双端>
- 时长/缓动：<默认值，如 400ms cubic-bezier(0.22,1,0.36,1)>
- 性能：<合成层 / 重排>
- 依赖：<无 / IO / WAAPI / View Transitions / 库名>
- 说明：<一两句：适合什么场景、什么时候别用、注意点>

```css
/* 可直接粘贴的最小实现，含 reduced-motion 降级 */
```
````

索引行格式：

```markdown
| <id> | <名称> | <区块> | <风格> | <触发> | <端> | <性能> | <文件名> |
```
